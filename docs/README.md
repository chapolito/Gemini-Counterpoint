# Docs assets

Add preview media for the main README here:

- `preview.gif` — preferred (short loop of underline + popover)
- `preview.mp4` / `preview.webm` — optional video
- `preview.png` — still screenshot

Then link it from the root `README.md` Preview section, for example:

```markdown
![Google Counterpoint preview](docs/preview.gif)
```
